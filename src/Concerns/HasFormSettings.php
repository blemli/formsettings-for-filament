<?php

namespace Blemli\FormSettings\Concerns;

/**
 * Marker trait: opts a Filament page with a form into the
 * formsettings gear. No methods needed — discovery, storage and
 * rendering are handled by the FormSettingsPlugin.
 */
trait HasFormSettings {}
